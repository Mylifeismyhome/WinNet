export ROOT_DIR=${PWD}/extern/OpenSSL/src

configure:
	${ROOT_DIR}/openssl-master/config -fPIC shared --prefix=/usr --openssldir=/etc/ssl

build:
	${MAKE}

install:
	${MAKE} install INSTALL_PREFIX=${ROOT_DIR}/bin/
